<h1>All Movies Listed Here!</h1>
<!--

 @index = 0

 loop @index while @movies < @movies.length
  @movies.slice(@index, 3)

  @index +=3
  end

<div class="container">
  @movies.slice(0,3) -->
  <% @movies.each do |movie|  %>
  <div>
  <a href= <%= movie_path(movie) %> >
  <img src= <%= movie.poster %> alt="Movie" style="width:120px;height:120px;border:0;">
</a>
  <ul>
    <li><%= movie.name %>
    <li><%= movie.ratings %>
  </ul>
  </div>
  <% end  %>
